json_schema_general = {
    "type": "object",
    "properties": {
        "response": {
            "type": "string",
            "description": "The response from the language model.",
        }
    },
    "required": ["response"],
}
